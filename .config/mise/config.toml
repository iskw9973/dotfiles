[settings]
disable_default_registry = false
experimental = true
verbose = false
quiet = true

# グローバルにツールを置かない方針。
# 理由: グローバルのデフォルトはプロジェクト側の宣言漏れを隠してしまい、
# 「自分のマシンでだけ動く」状態を生む。ツールは各プロジェクトの
# mise.toml で宣言する。
#
# 一時的に必要なときは `mise exec node@24 -- <cmd>` を使う。
